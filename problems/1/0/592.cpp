#include <stdio.h>
int main()
{
	int N ; 
	scanf("%d", &N);
        if(N<1)
        {
                printf("-1");
                return 0;
        }
        long long sum=0;
	for (int i = 0; i <= a; ++i)
	{
		sum += i;
	}
	printf("%lld", sum);
	return 0;
}
